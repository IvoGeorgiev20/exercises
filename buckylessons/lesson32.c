#include <stdio.h>

int main()
{
    int tuna = 'g';

    if( isalpha(tuna) )
        {
            printf("%c is a letter", tuna);
               }else{
            if(isdigit(tuna) )
          {
        printf("%c is a number, tuna");
        }else{
    printf("%c is a OMG WTF is sthat!?", tuna);
  }

}

    return 0;
}
